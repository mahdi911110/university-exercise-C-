#include<iostream>
#include<cstring>
int string_length(char *);
using namespace std;
// calculate string length
int main(){
    char name;
    cout << "length is " << string_length(&name);
}
int string_length(char *a){
    cout << "Enter your string: ";
    cin >> *a;
    size_t strlen(const char *a);
    cout << "Length is ";
    return *a;
}